import 'package:flutter/material.dart';

class DiabeticCare extends StatelessWidget {
  const DiabeticCare({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Diabetic Care")),
    );
  }
}
